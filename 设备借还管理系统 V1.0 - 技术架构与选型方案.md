## 文档信息
+ **版本**: v1.0.0
+ **日期**: 2025-11-12
+ **撰写人**: CTO
+ **审核状态**: 待审核

---

## 1. 整体架构蓝图 (Architecture Blueprint)
### 1.1 架构概览
本系统采用基于 Electron 的桌面应用架构，结合现代 Web 技术栈，实现本地离线部署的设备借还管理系统。整体架构分为四个核心层次：

```plain
┌─────────────────────────────────────────────────────────┐
│                    Electron Shell                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │              Main Process (Node.js)               │  │
│  │  ┌─────────────────────────────────────────────┐ │  │
│  │  │        Data Service Layer (sql.js)          │ │  │
│  │  │  - Database Initialization                  │ │  │
│  │  │  - CRUD Operations                          │ │  │
│  │  │  - Data Persistence                         │ │  │
│  │  └─────────────────────────────────────────────┘ │  │
│  │                       ↕ IPC                       │  │
│  │  ┌─────────────────────────────────────────────┐ │  │
│  │  │         Renderer Process (Chromium)         │ │  │
│  │  │  ┌───────────────────────────────────────┐  │ │  │
│  │  │  │    Application Core (Umi.js + React)  │  │ │  │
│  │  │  │  - Route Management                   │  │ │  │
│  │  │  │  - State Management                   │  │ │  │
│  │  │  │  - UI Components (Ant Design)         │  │ │  │
│  │  │  │  - Business Logic                     │  │ │  │
│  │  │  └───────────────────────────────────────┘  │ │  │
│  │  └─────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 1.2 架构分层详解
#### 1.2.1 外壳/容器层 (Shell Layer)
**Electron** 作为应用的顶层外壳，提供以下核心能力：

+ **进程管理**: 创建并管理主进程（Main Process）和渲染进程（Renderer Process）
+ **原生能力**: 提供窗口管理、系统菜单、系统托盘、文件系统访问等原生桌面功能
+ **安全隔离**: 通过进程隔离保证数据安全，主进程拥有完整的 Node.js 权限，渲染进程受限运行
+ **跨平台支持**: 统一的 API 支持 Windows、macOS、Linux 三大平台

#### 1.2.2 应用核心层 (Application Core Layer)
**Umi.js + React** 运行在 Electron 的渲染进程中，构成应用的核心业务逻辑：

+ **路由系统**: Umi 的约定式路由自动生成路由配置，支持嵌套路由和权限控制
+ **状态管理**: 集成 dva/useModel 进行全局状态管理
+ **UI 渲染**: React 组件化开发，结合 Ant Design 快速构建企业级界面
+ **插件生态**: Umi 丰富的插件体系支持按需加载、国际化、权限管理等功能

#### 1.2.3 数据服务层 (Data Service Layer)
**sql.js** 运行在 Electron 主进程中，负责所有数据持久化操作：

+ **数据库引擎**: 完整的 SQLite 实现，支持标准 SQL 语法
+ **文件管理**: 数据库文件存储在用户数据目录（`app.getPath('userData')`）
+ **事务支持**: 保证数据一致性和完整性
+ **数据隔离**: 所有数据操作在主进程中执行，渲染进程无法直接访问

#### 1.2.4 通信桥梁 (IPC Bridge)
**Electron IPC** 实现前后端通信：

+ **主进程 API**: 通过 `ipcMain.handle` 暴露异步数据服务接口
+ **渲染进程调用**: 通过 `ipcRenderer.invoke` 调用主进程服务
+ **类型安全**: 定义统一的消息协议和数据格式
+ **错误处理**: 统一的异常捕获和错误返回机制

---

## 2. 技术选型详解与优势论证 (Rationale)
### 2.1 Electron - 桌面应用容器的不二之选
#### 选型理由
Electron 是本项目实现"本地离线部署桌面应用"的最佳选择，具体原因如下：

**技术成熟度**：Electron 已成为桌面应用开发的事实标准，众多知名应用（VSCode、Slack、Discord、Figma）都基于 Electron 构建，技术栈经过海量用户验证。

**Web 技术复用**：允许我们使用熟悉的 Web 技术栈（HTML、CSS、JavaScript）开发桌面应用，极大降低学习成本，提升开发效率。团队无需掌握原生桌面开发技术（C++、Swift、C#），即可交付专业的桌面应用。

**跨平台能力**：一套代码可编译为 Windows、macOS、Linux 三大平台的安装包，大幅减少跨平台适配工作量。对于本项目来说，虽然 PRD 主要面向 Windows，但 Electron 为未来的跨平台扩展留足空间。

**丰富的生态**：庞大的社区支持、完善的文档、成熟的工具链（electron-builder、electron-forge）以及大量的第三方库，能够快速解决开发中遇到的各类问题。

**原生能力集成**：提供完整的 Node.js 运行时，可以调用操作系统 API、访问文件系统、创建系统通知、自定义菜单等，满足本地离线应用的所有功能需求。

#### 架构优势
+ **双进程架构**: 主进程负责数据和系统资源管理，渲染进程专注于 UI，职责清晰，易于维护
+ **安全性**: 通过 contextIsolation 和 nodeIntegration 配置，确保渲染进程的安全隔离
+ **更新机制**: 内置 autoUpdater 模块，支持应用的自动更新

---

### 2.2 Umi.js - 企业级应用框架的首选
#### 为什么选择 Umi 而非 Vite/CRA？
虽然 Vite 和 Create React App (CRA) 都是流行的 React 开发工具，但 **Umi.js** 更适合本项目，原因如下：

**约定式路由**：Umi 基于文件系统的约定式路由，开发者只需按照约定的目录结构创建文件，即可自动生成路由配置。这大幅简化了路由管理，避免手动维护复杂的路由配置文件。

对比：

```javascript
// Vite/CRA 需要手动配置
import { BrowserRouter, Routes, Route } from 'react-router-dom';

<Routes>
  <Route path="/dashboard" element={<Dashboard />} />
  <Route path="/devices" element={<Devices />} />
  {/* 大量手动配置... */}
</Routes>
// Umi 约定式路由
// 只需创建文件：src/pages/dashboard.tsx, src/pages/devices.tsx
// 路由自动生成
```

**插件化架构**：Umi 拥有强大的插件体系，许多常用功能开箱即用（权限管理、国际化、dva 状态管理、antd 集成等），无需手动配置和集成。这对于企业级应用的规范性和开发效率至关重要。

**Ant Design 深度集成**：Umi 官方提供了 `@umijs/preset-react` 和 `umi-plugin-antd`，实现了与 Ant Design 的无缝集成，包括主题定制、按需加载、国际化等功能一键配置。

**企业级规范**：Umi 默认集成了 TypeScript、ESLint、Prettier 等开发规范工具，并提供了统一的配置入口（`.umirc.ts`），确保项目代码质量和一致性。

**构建优化**：Umi 底层基于 Webpack，内置了大量构建优化策略（代码分割、Tree Shaking、懒加载），并提供了 `umi build` 命令一键打包优化。

**官方生态支持**：Umi 由蚂蚁集团开源并长期维护，与 Ant Design、dva、dumi 等工具形成完整的前端工程化生态，技术栈统一，降低学习和维护成本。

#### 适配本项目的优势
+ **快速启动**: 通过 `@umijs/preset-electron` 插件，可快速搭建 Umi + Electron 项目
+ **开发体验**: 热更新、Mock 数据、TypeScript 支持，极大提升开发效率
+ **扩展性**: 插件化架构便于后续功能扩展（如权限控制、日志系统）

---

### 2.3 Ant Design - B 端 UI 的标准答案
#### 选型理由
Ant Design 是本项目 UI 组件库的最佳选择，理由如下：

**组件丰富**：Ant Design 提供了 60+ 开箱即用的高质量 React 组件，完美覆盖 PRD 中的所有 UI 需求：

+ **数据录入**: Form、Input、Select、DatePicker、InputNumber 等
+ **数据展示**: Table、List、Card、Statistic、Badge 等
+ **反馈**: Modal、Message、Notification、Popconfirm 等
+ **导航**: Menu、Breadcrumb、Tabs 等
+ **数据可视化**: 配合 Ant Design Charts 可快速实现看板图表

**设计语言成熟**：Ant Design 是蚂蚁集团的企业级 UI 设计语言，经过多年打磨，设计规范完善、视觉风格统一、用户体验优秀，非常适合本项目的 B 端管理系统定位。

**与 Umi 无缝集成**：Umi 官方提供了 Ant Design 的集成插件，支持主题定制、按需加载、国际化等功能，配置简单，开箱即用。

**TypeScript 支持**：Ant Design 使用 TypeScript 编写，提供完善的类型定义，与 Umi 的 TypeScript 配置无缝衔接，提升代码的可维护性和健壮性。

**社区活跃**：GitHub 上拥有 90k+ Star，社区活跃，文档详尽，问题响应迅速，遇到问题可快速找到解决方案。

#### 功能匹配度
针对 PRD 中的核心功能模块，Ant Design 提供的组件完全满足需求：

| PRD 功能模块 | Ant Design 组件 |
| :---: | :---: |
| 首页数据看板 | Statistic, Card, Charts |
| 设备列表查询 | Table, Form, Select, Input |
| 借出/归还操作 | Modal, Form, DatePicker, Button |
| 逾期提醒弹窗 | Modal, Table, Badge, Tag |
| 设备管理 CRUD | Table, Modal, Form, Popconfirm |
| 个人中心 | Form, Input.Password, Button |


---

### 2.4 sql.js - 本地数据库的最优解
#### 为什么选择 sql.js 而非原生 SQLite？
虽然 Electron 可以通过 Node.js 原生模块（如 `better-sqlite3` 或 `node-sqlite3`）直接调用 SQLite C 库，但 **sql.js** 才是本项目的最优选择，理由如下：

**零原生依赖**：sql.js 是通过 WebAssembly (WASM) 编译的 SQLite 实现，完全基于 JavaScript 运行，无需编译原生模块。这意味着：

+ **无需配置原生构建环境**: 不需要安装 Python、node-gyp、C++ 编译器等工具链
+ **无平台兼容性问题**: 原生模块需要为每个平台单独编译（Windows、macOS、Linux），而 sql.js 是纯 JavaScript，跨平台零适配
+ **打包体积可控**: 原生模块依赖系统库，sql.js 是独立的 WASM 文件，打包更简单

**易于集成**：sql.js 可以直接在 Electron 主进程的 Node.js 环境中使用，无需复杂的配置和构建流程，通过 npm 安装即可使用。

```javascript
// sql.js 使用示例
const initSqlJs = require('sql.js');
const fs = require('fs');

const SQL = await initSqlJs();
const db = new SQL.Database();

// 创建表
db.run("CREATE TABLE devices (id INTEGER PRIMARY KEY, name TEXT)");

// 插入数据
db.run("INSERT INTO devices VALUES (?, ?)", [1, "示波器"]);

// 查询数据
const result = db.exec("SELECT * FROM devices");

// 持久化到文件
const data = db.export();
fs.writeFileSync('database.sqlite', Buffer.from(data));
```

**完全符合"离线、易部署"的需求**：sql.js 无需依赖外部数据库服务，数据库文件是一个标准的 SQLite 文件，可以随应用一起打包和分发，完美匹配本地离线部署的场景。

**功能完整**：sql.js 支持完整的 SQLite 语法，包括事务、索引、触发器、视图等高级特性，完全满足本项目的数据管理需求。

**性能表现**：虽然是 WASM 实现，但 sql.js 的性能已经非常接近原生 SQLite，对于本项目的数据规模（设备数量、借还记录）完全够用。

#### 数据持久化方案
sql.js 在内存中运行数据库，需要手动实现持久化：

+ **启动时**: 从 `userData` 目录读取数据库文件到内存
+ **操作时**: 所有 CRUD 操作在内存数据库中执行
+ **关闭时**: 将内存数据库导出为文件保存到 `userData` 目录
+ **定期保存**: 可设置定时器（如每 5 分钟）自动保存，防止异常关闭导致数据丢失

---

## 3. 关键实现要点 (Key Implementation Points)
### 3.1 项目初始化
#### 3.1.1 使用模板快速启动
推荐使用 `@umijs/preset-electron` 插件快速搭建项目：

```bash
# 创建 Umi 项目
npm create umi@latest
# 选择 Ant Design Pro 模板

# 安装 Electron 插件
npm install @umijs/preset-electron -D

# 安装依赖
npm install

# 启动开发环境
npm run dev:electron
```

#### 3.1.2 手动整合项目结构
如需更精细的控制，可手动整合：

```plain
project-root/
├── electron/                 # Electron 主进程代码
│   ├── main.js              # 主进程入口
│   ├── preload.js           # 预加载脚本
│   └── services/            # 数据服务层
│       └── database.js      # sql.js 数据库服务
├── src/                     # Umi 应用代码
│   ├── pages/               # 页面组件（约定式路由）
│   ├── components/          # 公共组件
│   ├── services/            # API 服务封装
│   ├── models/              # 数据模型（dva）
│   └── app.tsx              # 应用入口
├── .umirc.ts                # Umi 配置文件
├── package.json
└── tsconfig.json
```

#### 3.1.3 关键配置
**Electron 主进程配置（electron/main.js）**：

```javascript
const { app, BrowserWindow, ipcMain } = require('electron');
const path = require('path');
const isDev = require('electron-is-dev');

let mainWindow;

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });

  const url = isDev
    ? 'http://localhost:8000'  // Umi 开发服务器
    : `file://${path.join(__dirname, '../dist/index.html')}`;
  
  mainWindow.loadURL(url);
}

app.whenReady().then(createWindow);
```

**Umi 配置（.umirc.ts）**：

```typescript
import { defineConfig } from 'umi';

export default defineConfig({
  nodeModulesTransform: {
    type: 'none',
  },
  routes: [
    { path: '/', redirect: '/dashboard' },
    { path: '/dashboard', component: '@/pages/Dashboard' },
    { path: '/devices', component: '@/pages/Devices' },
    { path: '/borrow', component: '@/pages/Borrow' },
    { path: '/returned', component: '@/pages/Returned' },
    { path: '/profile', component: '@/pages/Profile' },
  ],
  antd: {},
  dva: {},
  locale: {
    default: 'zh-CN',
    antd: true,
  },
});
```

---

### 3.2 数据持久化实现
#### 3.2.1 主进程数据库初始化
在主进程中初始化 sql.js 数据库：

```javascript
// electron/services/database.js
const initSqlJs = require('sql.js');
const fs = require('fs');
const path = require('path');
const { app } = require('electron');

let db = null;
const dbPath = path.join(app.getPath('userData'), 'database.sqlite');

async function initDatabase() {
  const SQL = await initSqlJs();
  
  // 检查数据库文件是否存在
  if (fs.existsSync(dbPath)) {
    const buffer = fs.readFileSync(dbPath);
    db = new SQL.Database(buffer);
  } else {
    db = new SQL.Database();
    createTables();
  }
  
  // 定期保存数据库
  setInterval(saveDatabase, 5 * 60 * 1000); // 每5分钟保存一次
}

function createTables() {
  // 设备表
  db.run(`
    CREATE TABLE IF NOT EXISTS devices (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      code TEXT UNIQUE NOT NULL,
      name TEXT NOT NULL,
      brand TEXT,
      model TEXT,
      price REAL,
      location TEXT,
      status TEXT DEFAULT 'available',
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);
  
  // 借出记录表
  db.run(`
    CREATE TABLE IF NOT EXISTS borrow_records (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      device_id INTEGER NOT NULL,
      borrower_name TEXT NOT NULL,
      borrower_class TEXT NOT NULL,
      borrower_student_id TEXT NOT NULL,
      borrower_phone TEXT NOT NULL,
      borrow_time DATETIME NOT NULL,
      return_deadline DATETIME NOT NULL,
      actual_return_time DATETIME,
      notified INTEGER DEFAULT 0,
      FOREIGN KEY (device_id) REFERENCES devices(id)
    )
  `);
  
  // 用户表
  db.run(`
    CREATE TABLE IF NOT EXISTS users (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      username TEXT UNIQUE NOT NULL,
      password TEXT NOT NULL,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
  `);
  
  // 插入默认用户（密码应加密，此处仅示例）
  db.run("INSERT INTO users (username, password) VALUES (?, ?)", 
    ['admin', 'admin123']);
}

function saveDatabase() {
  if (db) {
    const data = db.export();
    fs.writeFileSync(dbPath, Buffer.from(data));
    console.log('Database saved:', new Date().toISOString());
  }
}

// 应用退出时保存数据库
app.on('before-quit', () => {
  saveDatabase();
});

module.exports = {
  initDatabase,
  getDatabase: () => db,
  saveDatabase,
};
```

---

### 3.3 API 封装
#### 3.3.1 主进程暴露数据服务
通过 `ipcMain.handle` 暴露异步 API：

```javascript
// electron/main.js
const { ipcMain } = require('electron');
const { getDatabase, saveDatabase } = require('./services/database');

// 设备管理 API
ipcMain.handle('device:list', async (event, filters) => {
  const db = getDatabase();
  let sql = 'SELECT * FROM devices WHERE 1=1';
  const params = [];
  
  if (filters?.code) {
    sql += ' AND code LIKE ?';
    params.push(`%${filters.code}%`);
  }
  
  if (filters?.status) {
    sql += ' AND status = ?';
    params.push(filters.status);
  }
  
  const result = db.exec(sql, params);
  return result[0]?.values || [];
});

ipcMain.handle('device:create', async (event, device) => {
  const db = getDatabase();
  const { code, name, brand, model, price, location } = device;
  
  db.run(
    'INSERT INTO devices (code, name, brand, model, price, location) VALUES (?, ?, ?, ?, ?, ?)',
    [code, name, brand, model, price, location]
  );
  
  saveDatabase();
  return { success: true };
});

ipcMain.handle('device:update', async (event, id, device) => {
  const db = getDatabase();
  const { code, name, brand, model, price, location } = device;
  
  db.run(
    'UPDATE devices SET code=?, name=?, brand=?, model=?, price=?, location=? WHERE id=?',
    [code, name, brand, model, price, location, id]
  );
  
  saveDatabase();
  return { success: true };
});

ipcMain.handle('device:delete', async (event, id) => {
  const db = getDatabase();
  
  // 检查设备是否已借出
  const result = db.exec(
    'SELECT status FROM devices WHERE id = ?',
    [id]
  );
  
  if (result[0]?.values[0]?.[0] === 'borrowed') {
    return { success: false, message: '设备已借出，无法删除' };
  }
  
  db.run('DELETE FROM devices WHERE id = ?', [id]);
  saveDatabase();
  return { success: true };
});

// 借还记录 API
ipcMain.handle('borrow:create', async (event, record) => {
  const db = getDatabase();
  const {
    device_id,
    borrower_name,
    borrower_class,
    borrower_student_id,
    borrower_phone,
    borrow_time,
    return_deadline,
  } = record;
  
  db.run(
    `INSERT INTO borrow_records 
     (device_id, borrower_name, borrower_class, borrower_student_id, 
      borrower_phone, borrow_time, return_deadline) 
     VALUES (?, ?, ?, ?, ?, ?, ?)`,
    [device_id, borrower_name, borrower_class, borrower_student_id, 
     borrower_phone, borrow_time, return_deadline]
  );
  
  // 更新设备状态
  db.run('UPDATE devices SET status = ? WHERE id = ?', ['borrowed', device_id]);
  
  saveDatabase();
  return { success: true };
});

ipcMain.handle('borrow:return', async (event, recordId) => {
  const db = getDatabase();
  const now = new Date().toISOString();
  
  // 获取设备ID
  const result = db.exec(
    'SELECT device_id FROM borrow_records WHERE id = ?',
    [recordId]
  );
  const deviceId = result[0]?.values[0]?.[0];
  
  // 更新归还时间
  db.run(
    'UPDATE borrow_records SET actual_return_time = ? WHERE id = ?',
    [now, recordId]
  );
  
  // 更新设备状态
  db.run('UPDATE devices SET status = ? WHERE id = ?', ['available', deviceId]);
  
  saveDatabase();
  return { success: true };
});

// 统计数据 API
ipcMain.handle('stats:dashboard', async () => {
  const db = getDatabase();
  
  const totalDevices = db.exec('SELECT COUNT(*) FROM devices')[0].values[0][0];
  const borrowedDevices = db.exec(
    "SELECT COUNT(*) FROM devices WHERE status = 'borrowed'"
  )[0].values[0][0];
  
  const overdueDevices = db.exec(`
    SELECT COUNT(*) FROM borrow_records 
    WHERE actual_return_time IS NULL 
    AND datetime(return_deadline) < datetime('now')
  `)[0].values[0][0];
  
  return {
    totalDevices,
    borrowedDevices,
    availableDevices: totalDevices - borrowedDevices,
    overdueDevices,
  };
});
```

#### 3.3.2 渲染进程调用封装
在 Umi 项目中封装 IPC 调用：

```typescript
// src/services/device.ts
const { ipcRenderer } = window.require('electron');

export async function listDevices(filters?: any) {
  return await ipcRenderer.invoke('device:list', filters);
}

export async function createDevice(device: any) {
  return await ipcRenderer.invoke('device:create', device);
}

export async function updateDevice(id: number, device: any) {
  return await ipcRenderer.invoke('device:update', id, device);
}

export async function deleteDevice(id: number) {
  return await ipcRenderer.invoke('device:delete', id);
}

// src/services/borrow.ts
export async function createBorrowRecord(record: any) {
  return await ipcRenderer.invoke('borrow:create', record);
}

export async function returnDevice(recordId: number) {
  return await ipcRenderer.invoke('borrow:return', recordId);
}

// src/services/stats.ts
export async function getDashboardStats() {
  return await ipcRenderer.invoke('stats:dashboard');
}
```

#### 3.3.3 页面组件中使用
```typescript
// src/pages/Devices/index.tsx
import { useEffect, useState } from 'react';
import { Table, Button, Modal, Form, Input, message } from 'antd';
import { listDevices, createDevice, deleteDevice } from '@/services/device';

export default function Devices() {
  const [devices, setDevices] = useState([]);
  const [loading, setLoading] = useState(false);
  const [modalVisible, setModalVisible] = useState(false);
  const [form] = Form.useForm();

  useEffect(() => {
    loadDevices();
  }, []);

  const loadDevices = async () => {
    setLoading(true);
    try {
      const data = await listDevices();
      setDevices(data);
    } catch (error) {
      message.error('加载设备列表失败');
    } finally {
      setLoading(false);
    }
  };

  const handleCreate = async (values: any) => {
    try {
      await createDevice(values);
      message.success('新增设备成功');
      setModalVisible(false);
      form.resetFields();
      loadDevices();
    } catch (error) {
      message.error('新增设备失败');
    }
  };

  const handleDelete = async (id: number) => {
    const result = await deleteDevice(id);
    if (result.success) {
      message.success('删除成功');
      loadDevices();
    } else {
      message.error(result.message);
    }
  };

  const columns = [
    { title: '设备编号', dataIndex: 'code', key: 'code' },
    { title: '设备名称', dataIndex: 'name', key: 'name' },
    { title: '品牌', dataIndex: 'brand', key: 'brand' },
    { title: '状态', dataIndex: 'status', key: 'status' },
    {
      title: '操作',
      key: 'action',
      render: (text: any, record: any) => (
        <>
          <Button type="link">编辑</Button>
          <Button 
            type="link" 
            danger 
            onClick={() => handleDelete(record.id)}
          >
            删除
          </Button>
        </>
      ),
    },
  ];

  return (
    <div>
      <Button type="primary" onClick={() => setModalVisible(true)}>
        新增设备
      </Button>
      <Table 
        columns={columns} 
        dataSource={devices} 
        loading={loading}
        rowKey="id"
      />
      <Modal
        title="新增设备"
        visible={modalVisible}
        onCancel={() => setModalVisible(false)}
        onOk={() => form.submit()}
      >
        <Form form={form} onFinish={handleCreate}>
          <Form.Item name="code" label="设备编号" rules={[{ required: true }]}>
            <Input />
          </Form.Item>
          <Form.Item name="name" label="设备名称" rules={[{ required: true }]}>
            <Input />
          </Form.Item>
          {/* 其他表单项 */}
        </Form>
      </Modal>
    </div>
  );
}
```

---

## 4. 开发路线图 (Roadmap)
### 第一阶段：环境搭建与基础框架（Week 1）
**目标**：完成开发环境配置，搭建项目骨架，跑通 "Hello World"

**任务清单**：

- [ ] 安装开发环境（Node.js、npm/yarn、IDE）
- [ ] 创建 Umi 项目，集成 Ant Design
- [ ] 集成 Electron，配置主进程和渲染进程
- [ ] 配置 TypeScript、ESLint、Prettier
- [ ] 实现 Electron 窗口创建和 Umi 页面加载
- [ ] 验证热更新、打包功能正常

**交付物**：

+ 可运行的 Electron + Umi 项目
+ 开发环境配置文档

---

### 第二阶段：核心数据层实现（Week 2）
**目标**：完成基于 sql.js 的数据库设计与数据服务层开发

**任务清单**：

- [ ] 设计数据库表结构（设备表、借还记录表、用户表）
- [ ] 实现主进程 sql.js 初始化和持久化逻辑
- [ ] 封装设备管理 CRUD API（create、read、update、delete）
- [ ] 封装借还记录管理 API
- [ ] 封装统计数据 API（首页看板）
- [ ] 封装用户认证 API（登录、修改密码）
- [ ] 编写单元测试（可选）

**交付物**：

+ 完整的数据服务层代码
+ API 接口文档

---

### 第三阶段：功能模块开发（Week 3-5）
**目标**：按照 PRD 逐一实现各个功能模块

#### 3.1 用户认证模块（Week 3）
- [ ] 登录页面 UI 开发
- [ ] 登录逻辑实现（调用主进程 API）
- [ ] 登录状态管理（dva/useModel）
- [ ] 路由权限控制（未登录跳转登录页）

#### 3.2 首页看板模块（Week 3）
- [ ] 关键指标卡片开发（设备总数、在借、可借、逾期）
- [ ] 设备类型分布图表（饼图/环形图）
- [ ] 设备借出状态图表（柱状图）
- [ ] 归还倒计时列表（时间轴）
- [ ] 逾期提醒弹窗开发
- [ ] 数据刷新逻辑

#### 3.3 借出设备记录模块（Week 4）
- [ ] 借出设备列表页面（Table + 查询条件）
- [ ] 新增借出设备弹窗
- [ ] 归还设备确认弹窗
- [ ] 数据分页和排序功能

#### 3.4 已还设备记录模块（Week 4）
- [ ] 已还设备列表页面
- [ ] 查询条件和筛选功能
- [ ] 删除记录功能

#### 3.5 设备管理模块（Week 5）
- [ ] 设备列表查询页面
- [ ] 新增设备弹窗（两种模式）
- [ ] 编辑设备弹窗
- [ ] 删除设备确认弹窗
- [ ] Excel 批量导入功能（可选）

#### 3.6 个人中心模块（Week 5）
- [ ] 修改密码页面
- [ ] 密码验证逻辑

**交付物**：

+ 所有功能模块的前端页面和业务逻辑
+ 功能演示视频

---

### 第四阶段：打包、测试与优化（Week 6）
**目标**：完成应用打包、多平台测试、性能优化

**任务清单**：

- [ ] 配置 electron-builder 打包脚本
- [ ] Windows 平台打包测试（.exe 安装包）
- [ ] macOS 平台打包测试（可选）
- [ ] 应用图标和启动画面设计
- [ ] 应用自动更新功能集成（可选）
- [ ] 性能优化（首屏加载、数据查询）
- [ ] 安全加固（数据库加密、密码加密）
- [ ] 编写用户手册和部署文档

**交付物**：

+ Windows 安装包（.exe）
+ 用户手册
+ 部署文档

---

## 5. 风险评估与应对策略
### 5.1 技术风险
| 风险项 | 影响 | 应对策略 |
| :---: | :---: | :---: |
| Electron 应用体积过大 | 用户下载和安装体验差 | 1. 使用 electron-builder 优化打包配置   2. 启用代码分割和懒加载   3. 移除未使用的依赖 |
| sql.js 性能瓶颈 | 数据量大时查询缓慢 | 1. 合理设计数据库索引   2. 实现数据分页和虚拟滚动   3. 定期清理历史数据 |
| IPC 通信异常 | 前后端数据不一致 | 1. 统一的错误处理机制   2. 添加请求超时和重试逻辑   3. 完善的日志记录 |


### 5.2 开发风险
| 风险项 | 影响 | 应对策略 |
| :---: | :---: | :---: |
| 团队对 Electron 不熟悉 | 开发效率低 | 1. 提供 Electron 培训材料   2. 安排技术分享会   3. 先完成 Demo 项目熟悉技术栈 |
| PRD 需求变更频繁 | 开发进度延期 | 1. 与产品方确认需求优先级   2. 采用敏捷开发，快速迭代   3. 预留需求变更缓冲时间 |


---

## 6. 总结
本技术方案基于 **Electron + Umi.js + React + Ant Design + sql.js** 的技术栈，充分发挥了现代 Web 技术在桌面应用开发中的优势，既满足了 PRD 中"本地离线部署"的核心需求，又确保了开发效率和代码质量。

**核心优势**：

1. **技术成熟稳定**：所有技术选型都是经过大规模验证的成熟方案
2. **开发效率高**：Umi + Ant Design 的企业级框架大幅提升开发效率
3. **跨平台能力强**：一套代码支持 Windows、macOS、Linux
4. **易于部署维护**：本地离线运行，零依赖外部服务
5. **扩展性好**：插件化架构便于后续功能扩展

**下一步行动**：

+ 组织技术评审会议，确认技术方案
+ 按照路线图启动第一阶段开发
+ 建立每周进度同步机制

